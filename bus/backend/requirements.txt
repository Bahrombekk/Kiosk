# Server (admin) dasturi kutubxonalari
fastapi>=0.110
uvicorn[standard]>=0.29   # WebSocket (websockets) shu paket bilan keladi
PyQt6>=6.6                # admin desktop oynasi
PyQt6-WebEngine>=6.6      # Bekat dialogidagi oflayn xarita (ixtiyoriy — bo'lmasa xaritasiz ishlaydi)
cryptography>=42.0        # Ed25519 imzo (discovery) + TLS sertifikat (security.py)
