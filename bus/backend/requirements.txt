# Avtobus backend — HEADLESS (PyQt YO'Q; faqat veb + API + bulut agenti).
# Poyezd serveridan farqli: PyQt6 / PyQt6-WebEngine olib tashlandi — avtobusда
# desktop oyna yo'q, exe ham shuncha yengil bo'ladi.
fastapi>=0.110
uvicorn[standard]>=0.29   # WebSocket (websockets) shu paket bilan keladi
cryptography>=42.0        # Ed25519 imzo (litsenziya/identity) + TLS sertifikat
