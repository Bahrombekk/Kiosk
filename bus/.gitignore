# Sirlar / muhit
.env
.env.*
!.env.example
*.pem
*.key

# Ma'lumot (baza, kontent, loglar)
backend/*.db
backend/*.db-*
backend/content/
backend/logs/
backend/weather_cache.json
backend/cloud.txt
backend/license.key*

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Web (Nuxt)
web/node_modules/
web/.nuxt/
web/.output/
*.log

# Build artefaktlari (build.ps1 / PyInstaller / Inno)
build/
dist/
release/
Output/
# node.exe / ffmpeg.exe / nssm.exe — og'ir binar, repoga kirmaydi
vendor/*.exe
!vendor/README.md
backend/*.pyd
backend/*.pyi
