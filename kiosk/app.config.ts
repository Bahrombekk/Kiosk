export default defineAppConfig({
  ui: {
    modal: {
      slots: {
        content: "!ring-0 border-0 !shadow-none bg-transparent",
        overlay: "bg-[rgba(0, 0, 0, 0.02)]",
      },
    },
  },
});
